import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Pro-Finder Belize | Trusted Tradesman Marketplace",
  description: "Find verified tradesmen in Belize. Post jobs, get quotes, and pay securely with our escrow system. Plumbing, electrical, HVAC, IT support and more.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
